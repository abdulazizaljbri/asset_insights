[project]
name = "asset_insights"
authors = [{ name = "Asset Insights Team" }]
description = "Professional BI reports & dashboards for ERPNext v15 - Fixed Assets"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
